# Environment variables

# Local development
## Optional: defaults to 5173 if not set
VITE_DEV_PORT=5173

## Optional: defaults to localhost and 127.0.0.1 if not set
VITE_DEV_ALLOWED_HOSTS=localhost,127.0.0.1
